version: "3.9"
services:
  frontend:
    build: .
    args:
      - VITE_BASE_URL=${VITE_BASE_URL}
    container_name: react-frontend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped